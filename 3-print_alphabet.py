def lower_alphabet():
    for i in range(97, 123):# this uses the ascii values that correspond to each letter
        print(chr(i), end="")# the chr converts the i into characte value.
lower_alphabet()# we now call the function.